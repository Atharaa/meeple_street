def multiplication(left_factor, right_factor):
    return left_factor * right_factor
def addition(left_factor, right_factor):
    return left_factor + right_factor
def soustraction(left_factor, right_factor):
    return left_factor - right_factor
def division(left_factor, right_factor):
    return left_factor / right_factor